TRADING STRATEGY VAULT — GROQ ONLY

AI mode: Single AI (Groq only)
Model: qwen/qwen3.6-27b

AI Analysis:
- Upload a Quotex chart screenshot.
- Market/pair is detected automatically from visible chart text.
- Groq analyzes the screenshot and returns UP, DOWN or STOP with confidence, reason, risk and timeframe.
- No OpenAI, Gemini or Twelve Data API is used.

Future Signals:
- Groq only.
- Market/pair is detected automatically from the screenshot.

CONFIGURATION:
1. Open ai-config.js.
2. Replace PASTE_GROQ_API_KEY_HERE with your own Groq API key.
3. Keep the file beside index.html on GitHub/hosting.

SECURITY:
Browser-side API keys are visible to visitors. For a real production secret, use a backend/proxy. Never publish a key you have exposed or shared.

Educational notice: AI output is an estimate, not guaranteed trading advice.
